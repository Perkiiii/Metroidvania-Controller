using UnityEngine;

// Marks a position where the hero respawns after normal death.
// Wired to SaveManager in Milestone 4; stub exists now so HazardZone and
// CheckpointInteractable can hold references to it without compile errors.
public sealed class RespawnMarker : MonoBehaviour { }
